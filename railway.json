{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo 'Starting openclaw gateway...' && mkdir -p /data/.openclaw || true && node openclaw.mjs gateway --allow-unconfigured --bind lan --port ${PORT:-8080}",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
